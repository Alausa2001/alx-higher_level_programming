#!/usr/bin/python3
def uppercase(str):
    for a in str:
        if ord(a) >= 97 and ord(a) <= 122:
            a = ord(a) - 32
        print("{}".format(chr(a)), end='')
    print()
no_of_daysuser = 365 * age_of_userconvert
noofweeksuser = 52 * age_of_userconvert
noofmonthuser = 12 * age_of_userconvert
print(f("what {noofweeksuser}"))
